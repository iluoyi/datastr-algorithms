package leetcode;

public class FlattenBinaryTreeToLinkedList {
    public void flatten(TreeNode root) {
        if (root != null) {
        	//preoder(root);
        }
    }
    
    public void preoder(TreeNode crt, TreeNode pre) {
    	TreeNode left = crt.left;
    	 
    }
}
